namespace $ {
  $mol_test({
    "detected stack"($) {
      //   const app = $scale_form_detected_stack.make({ $ });
      //   const autoFixtures: $scale_modelDetectedAuto[] = [
      //     { id: "TEST1", number: "А111АА102", direction: "IN" },
      //     { id: "TEST2", number: "А111АА102", direction: "OUT" },
      //   ];
      //   //   const list = this.detected_auto_stack_list();
      //   // return list.map((item) => this.Detected_auto_stack_row(item));
      //   //   app.detected_auto_stack(
      //   //     autoFixtures.map((auto) => app.Detected_auto_stack_row(auto))
      //   //   );
      //   app.detected_auto_stack_list(autoFixtures);
      //   console.log(
      //     "fff",
      //     autoFixtures,
      //     app.Detected_auto_stack(),
      //     app.detected_auto_stack_list()
      //   );
      //   const list = app.Detected_auto_stack();
      //   list[0].click();
      //   const app.Detected_auto_stack();
      //   app.state_key
      // const centrifuge = app.cent
      //   const prev = app.Photo().uri();
      //   app.Next().click();
      //   const next = app.Photo().uri();
      //   $mol_assert_unique(prev, next);
    },
  });
}
